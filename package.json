{
  "name": "redux-storage-decorator-immutable-filter",
  "version": "1.0.1",
  "description": "A better filter decorator to use with redux-storage (if your state is an immutable Map)",
  "main": "build/index.js",
  "scripts": {
    "test": "jest --no-cache",
    "build": "babel src --out-dir build",
    "prepublish": "npm test && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/danscan/redux-storage-decorator-immutable-filter"
  },
  "homepage": "https://github.com/danscan/redux-storage-decorator-immutable-filter",
  "keywords": [
    "redux",
    "redux-storage",
    "redux-storage-decorator",
    "immutablejs"
  ],
  "author": "Dan Scanlon",
  "files": [
    "build/",
    "src",
    "!**/__tests__/**"
  ],
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-eslint": "^4.1.6",
    "babel-jest": "^12.0.2",
    "babel-polyfill": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "eslint": "^1.10.3",
    "eslint-config-airbnb": "^2.1.1",
    "immutable": "^3.7.6",
    "jest-cli": "^12.0.2"
  },
  "dependencies": {}
}
